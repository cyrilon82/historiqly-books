---
import SidebarLayout from '../layouts/SidebarLayout.astro';
import EmailCapture from '../components/EmailCapture.astro';
---

<SidebarLayout title="AI Casefiles — Evidence-Based History">
  <div class="home-page">
    <!-- Hero -->
    <section class="hero">
      <div class="hero-glow"></div>
      <div class="hero-grid-pattern"></div>
      <div class="hero-inner">
        <div class="hero-badge">
          <span class="hero-badge-dot"></span>
          Launching April 15th
        </div>
        <h1>History's Greatest Mysteries.<br/><span class="text-accent">Investigated by AI.</span></h1>
        <p class="hero-lead">
          Every casefile runs through our Evidence Engine: sources graded, timelines rebuilt,
          theories ranked. No spin. Just evidence.
        </p>
        <div class="hero-cta">
          <a href="#" class="btn-hero-primary" data-subscription>
            <span>Launching April 15th</span>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </a>
          <a href="/vol/hoaxes" class="btn-hero-secondary">
            <span class="soon-badge">Soon</span>
            Preview Vol 1
          </a>
        </div>
        <div class="hero-stats">
          <div class="hero-stat">
            <span class="hero-stat-value">11</span>
            <span class="hero-stat-label">Volumes</span>
          </div>
          <div class="hero-stat-divider"></div>
          <div class="hero-stat">
            <span class="hero-stat-value">220</span>
            <span class="hero-stat-label">Casefiles</span>
          </div>
          <div class="hero-stat-divider"></div>
          <div class="hero-stat">
            <span class="hero-stat-value">PDF</span>
            <span class="hero-stat-label">+ ePub</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Value Prop -->
    <section class="value-section">
      <div class="value-grid">
        <div class="value-block">
          <div class="value-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
            </svg>
          </div>
          <h3>Most history books ask you to trust the author.</h3>
          <p>They give you a polished narrative. But what's left out? The conflicting reports, the unreliable witnesses, the inconvenient facts that didn't fit the story.</p>
        </div>
        <div class="value-block highlight">
          <div class="value-icon accent">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
            </svg>
          </div>
          <h3>We ask you to trust the <span class="text-accent">evidence</span>.</h3>
          <p>AI Casefiles show you everything. Every source is graded. Every claim is tested. Every theory is scored against the evidence—so you can decide for yourself.</p>
        </div>
      </div>
    </section>

    <!-- Divider -->
    <div class="section-divider"></div>

    <!-- Evidence Engine -->
    <section class="engine-section">
      <div class="section-label">The Method</div>
      <h2>The Evidence Engine</h2>
      <p class="section-lead">Every case goes through the same forensic process.</p>

      <div class="engine-grid">
        <div class="engine-step">
          <div class="step-header">
            <div class="step-num">01</div>
            <div class="step-line"></div>
          </div>
          <h4>Source Grading</h4>
          <p>Every document, witness, and claim is graded A–D based on reliability, proximity, and corroboration.</p>
          <div class="step-tag">Reliability Score</div>
        </div>
        <div class="engine-step">
          <div class="step-header">
            <div class="step-num">02</div>
            <div class="step-line"></div>
          </div>
          <h4>Timeline Reconstruction</h4>
          <p>We rebuild the chronology using only A and B-grade sources. Gaps are marked, not filled with speculation.</p>
          <div class="step-tag">Verified Only</div>
        </div>
        <div class="engine-step">
          <div class="step-header">
            <div class="step-num">03</div>
            <div class="step-line last"></div>
          </div>
          <h4>Theory Scoreboard</h4>
          <p>Each competing theory is scored 0–100 based on evidence weight. We show what would change the conclusion.</p>
          <div class="step-tag">0–100 Score</div>
        </div>
      </div>
    </section>

    <!-- Divider -->
    <div class="section-divider"></div>

    <!-- What's Inside -->
    <section class="contents-section">
      <div class="section-label">What You Get</div>
      <h2>The Full Library</h2>

      <div class="contents-grid">
        <div class="content-card">
          <div class="content-icon large-num">220</div>
          <h4>Deep-Dive Casefiles</h4>
          <p>3,500+ words each. Full forensic treatment.</p>
        </div>
        <div class="content-card">
          <div class="content-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M9 17V7m0 10a2 2 0 01-2 2H5a2 2 0 01-2-2V7a2 2 0 012-2h2a2 2 0 012 2m0 10a2 2 0 002 2h2a2 2 0 002-2M9 7a2 2 0 012-2h2a2 2 0 012 2m0 10V7m0 10a2 2 0 002 2h2a2 2 0 002-2V7a2 2 0 00-2-2h-2a2 2 0 00-2 2"/>
            </svg>
          </div>
          <h4>Evidence Maps</h4>
          <p>Visual breakdown with A–D source grades.</p>
        </div>
        <div class="content-card">
          <div class="content-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <circle cx="12" cy="12" r="10"/>
              <polyline points="12 6 12 12 16 14"/>
            </svg>
          </div>
          <h4>Timelines</h4>
          <p>Verified chronology from high-grade sources only.</p>
        </div>
        <div class="content-card">
          <div class="content-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>
            </svg>
          </div>
          <h4>Theory Scores</h4>
          <p>Confidence percentages on each explanation.</p>
        </div>
      </div>
    </section>

    <!-- Divider -->
    <div class="section-divider"></div>

    <!-- Pricing -->
    <section class="pricing-section">
      <div class="section-label">Pricing</div>
      <h2>Unlock the Full Library</h2>
      <p class="section-lead">New volumes drop monthly. Subscribe to get them all.</p>

      <div class="pricing-grid">
        <div class="pricing-card">
          <div class="pricing-header">
            <h3>Monthly</h3>
            <p>Full access, flexible commitment</p>
          </div>
          <div class="price">$4.99<span class="price-period">/mo</span></div>
          <ul class="feature-list">
            <li>All current volumes</li>
            <li>New volumes as they release</li>
            <li>PDF + ePub downloads</li>
            <li>Cancel anytime</li>
          </ul>
          <button class="btn-pricing" data-subscription>Launching April 15th</button>
        </div>

        <div class="pricing-card featured">
          <div class="featured-badge">Save 35%</div>
          <div class="pricing-header">
            <h3>Yearly</h3>
            <p>Best value for serious investigators</p>
          </div>
          <div class="price">$39<span class="price-period">/yr</span></div>
          <ul class="feature-list">
            <li>Everything in Monthly</li>
            <li>Early access to new volumes</li>
            <li>Bonus deep-dive casefiles</li>
            <li>Subscriber-only content</li>
          </ul>
          <button class="btn-pricing featured" data-subscription>Launching April 15th</button>
        </div>
      </div>

      <div class="free-preview">
        <p>Not ready to commit? <a href="/vol/hoaxes"><span class="soon-badge">Soon</span> Preview Hoaxes & Frauds <span class="arrow">→</span></a></p>
      </div>
    </section>

    <!-- Divider -->
    <div class="section-divider"></div>

    <!-- FAQ -->
    <section class="faq-section">
      <h2>Questions</h2>
      <div class="faq-grid">
        <details class="faq-item">
          <summary>Is this written by AI?</summary>
          <div class="faq-answer">
            <p>Yes—100%. Every casefile is researched, analyzed, and written by the best AI models on the market. As these models improve, so do our investigations. Better AI = deeper research, sharper analysis, more rigorous evidence scoring.</p>
          </div>
        </details>
        <details class="faq-item">
          <summary>What formats are included?</summary>
          <div class="faq-answer">
            <p>PDF and ePub. Both are DRM-free—read them on any device.</p>
          </div>
        </details>
        <details class="faq-item">
          <summary>How often do new volumes come out?</summary>
          <div class="faq-answer">
            <p>We release one new volume per month. Yearly subscribers get early access before the public release.</p>
          </div>
        </details>
        <details class="faq-item">
          <summary>Can I cancel anytime?</summary>
          <div class="faq-answer">
            <p>Yes. Cancel your subscription anytime from your account. You'll keep access until the end of your billing period.</p>
          </div>
        </details>
      </div>
    </section>

    <!-- Email Capture -->
    <section class="email-section">
      <EmailCapture />
    </section>
  </div>
</SidebarLayout>

<style>
  .home-page {
    max-width: 820px;
    margin: 0 auto;
  }

  .text-accent {
    background: linear-gradient(135deg, #5DB585 0%, #7EDBA8 50%, #5DB585 100%);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: shimmer-text 4s ease-in-out infinite;
  }

  @keyframes shimmer-text {
    0%, 100% { background-position: 0% center; }
    50% { background-position: 200% center; }
  }

  /* Section Dividers */
  .section-divider {
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(93, 181, 133, 0.15), transparent);
    margin: 1rem 0;
  }

  /* ============================================
     HERO SECTION
     ============================================ */
  .hero {
    text-align: center;
    padding: 4rem 0 3rem;
    position: relative;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    top: -80px;
    left: 50%;
    transform: translateX(-50%);
    width: 600px;
    height: 400px;
    background: radial-gradient(ellipse, rgba(93, 181, 133, 0.08) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-grid-pattern {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(93, 181, 133, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(93, 181, 133, 0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 50% 70% at 50% 30%, black, transparent);
    pointer-events: none;
  }

  .hero-inner {
    position: relative;
    z-index: 1;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.4rem 1.125rem;
    background: rgba(93, 181, 133, 0.08);
    border: 1px solid rgba(93, 181, 133, 0.2);
    border-radius: 100px;
    font-size: 0.6875rem;
    font-weight: 600;
    color: #5DB585;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 1.75rem;
  }

  .hero-badge-dot {
    width: 6px;
    height: 6px;
    background: #5DB585;
    border-radius: 50%;
    animation: pulse-dot 2s ease-in-out infinite;
  }

  @keyframes pulse-dot {
    0%, 100% { opacity: 0.4; box-shadow: 0 0 0 0 rgba(93, 181, 133, 0.4); }
    50% { opacity: 1; box-shadow: 0 0 0 4px rgba(93, 181, 133, 0); }
  }

  .hero h1 {
    font-size: clamp(2.25rem, 5.5vw, 3.25rem);
    font-weight: 800;
    line-height: 1.08;
    margin-bottom: 1.5rem;
    letter-spacing: -0.03em;
  }

  .hero-lead {
    font-size: 1.0625rem;
    color: #999;
    max-width: 520px;
    margin: 0 auto 2.25rem;
    line-height: 1.7;
  }

  .hero-cta {
    display: flex;
    gap: 0.875rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2.5rem;
  }

  /* Hero Buttons */
  .btn-hero-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.75rem;
    background: #5DB585;
    color: #0a0a0a;
    font-weight: 700;
    font-size: 0.9375rem;
    border-radius: 10px;
    text-decoration: none;
    transition: all 0.25s;
    box-shadow: 0 0 20px rgba(93, 181, 133, 0.15), 0 4px 12px rgba(0, 0, 0, 0.3);
  }

  .btn-hero-primary:hover {
    background: #6FC494;
    transform: translateY(-2px);
    box-shadow: 0 0 30px rgba(93, 181, 133, 0.25), 0 8px 20px rgba(0, 0, 0, 0.4);
  }

  .btn-hero-primary svg {
    transition: transform 0.25s;
  }

  .btn-hero-primary:hover svg {
    transform: translateX(2px);
  }

  .btn-hero-secondary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.75rem;
    background: rgba(255, 255, 255, 0.03);
    color: #ccc;
    font-weight: 600;
    font-size: 0.9375rem;
    border-radius: 10px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    text-decoration: none;
    transition: all 0.25s;
  }

  .btn-hero-secondary:hover {
    border-color: rgba(255, 255, 255, 0.2);
    background: rgba(255, 255, 255, 0.06);
    color: #fff;
  }

  .soon-badge {
    display: inline-block;
    padding: 0.125rem 0.4375rem;
    background: rgba(255, 255, 255, 0.08);
    color: #888;
    font-size: 0.5625rem;
    font-weight: 700;
    text-transform: uppercase;
    border-radius: 4px;
    letter-spacing: 0.02em;
  }

  /* Hero Stats */
  .hero-stats {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
  }

  .hero-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.125rem;
  }

  .hero-stat-value {
    font-size: 1.125rem;
    font-weight: 800;
    color: #fff;
    letter-spacing: -0.02em;
  }

  .hero-stat-label {
    font-size: 0.6875rem;
    color: #666;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .hero-stat-divider {
    width: 1px;
    height: 28px;
    background: rgba(255, 255, 255, 0.08);
  }

  /* ============================================
     VALUE PROPOSITION SECTION
     ============================================ */
  .value-section {
    padding: 2.5rem 0;
  }

  .value-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.25rem;
  }

  .value-block {
    padding: 1.75rem;
    border-radius: 14px;
    background: linear-gradient(180deg, #131313 0%, #101010 100%);
    border: 1px solid rgba(255, 255, 255, 0.06);
    transition: border-color 0.3s, transform 0.3s;
  }

  .value-block:hover {
    border-color: rgba(255, 255, 255, 0.1);
    transform: translateY(-2px);
  }

  .value-block.highlight {
    border-color: rgba(93, 181, 133, 0.25);
    background: linear-gradient(180deg, rgba(93, 181, 133, 0.06) 0%, rgba(93, 181, 133, 0.02) 100%);
  }

  .value-block.highlight:hover {
    border-color: rgba(93, 181, 133, 0.4);
  }

  .value-icon {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.04);
    border-radius: 10px;
    margin-bottom: 1rem;
    color: #666;
  }

  .value-icon.accent {
    background: rgba(93, 181, 133, 0.1);
    color: #5DB585;
  }

  .value-block h3 {
    font-size: 1.0625rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    line-height: 1.35;
  }

  .value-block p {
    font-size: 0.9375rem;
    color: #777;
    line-height: 1.65;
    margin: 0;
  }

  /* ============================================
     SECTION LABELS & HEADINGS
     ============================================ */
  .section-label {
    font-size: 0.6875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: #5DB585;
    margin-bottom: 0.5rem;
  }

  section h2 {
    font-size: 1.875rem;
    font-weight: 800;
    margin-bottom: 0.5rem;
    letter-spacing: -0.025em;
  }

  .section-lead {
    color: #777;
    font-size: 1rem;
    margin-bottom: 2.25rem;
    line-height: 1.6;
  }

  /* ============================================
     EVIDENCE ENGINE SECTION
     ============================================ */
  .engine-section {
    padding: 3rem 0;
  }

  .engine-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .engine-step {
    padding: 1.5rem;
    background: linear-gradient(180deg, #131313 0%, #0f0f0f 100%);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 14px;
    transition: all 0.3s;
    position: relative;
  }

  .engine-step:hover {
    border-color: rgba(93, 181, 133, 0.2);
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
  }

  .step-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .step-num {
    font-size: 0.8125rem;
    font-weight: 800;
    color: #5DB585;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(93, 181, 133, 0.1);
    border-radius: 8px;
    flex-shrink: 0;
  }

  .step-line {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, rgba(93, 181, 133, 0.3), rgba(93, 181, 133, 0.05));
  }

  .step-line.last {
    background: linear-gradient(90deg, rgba(93, 181, 133, 0.3), transparent);
  }

  .engine-step h4 {
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
  }

  .engine-step p {
    font-size: 0.8125rem;
    color: #777;
    line-height: 1.6;
    margin: 0 0 1rem;
  }

  .step-tag {
    display: inline-block;
    padding: 0.25rem 0.625rem;
    background: rgba(93, 181, 133, 0.06);
    border: 1px solid rgba(93, 181, 133, 0.12);
    border-radius: 6px;
    font-size: 0.6875rem;
    font-weight: 600;
    color: #5DB585;
    letter-spacing: 0.02em;
  }

  /* ============================================
     WHAT YOU GET SECTION
     ============================================ */
  .contents-section {
    padding: 3rem 0;
  }

  .contents-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
  }

  .content-card {
    padding: 1.5rem 1.25rem;
    background: linear-gradient(180deg, #131313 0%, #0f0f0f 100%);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 14px;
    text-align: center;
    transition: all 0.3s;
  }

  .content-card:hover {
    border-color: rgba(93, 181, 133, 0.2);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
  }

  .content-icon {
    width: 48px;
    height: 48px;
    margin: 0 auto 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(93, 181, 133, 0.08);
    border-radius: 12px;
    color: #5DB585;
  }

  .content-icon.large-num {
    font-size: 1.25rem;
    font-weight: 800;
    color: #5DB585;
  }

  .content-card h4 {
    font-size: 0.9375rem;
    font-weight: 700;
    margin-bottom: 0.375rem;
  }

  .content-card p {
    font-size: 0.8125rem;
    color: #666;
    margin: 0;
    line-height: 1.5;
  }

  /* ============================================
     PRICING SECTION
     ============================================ */
  .pricing-section {
    padding: 3rem 0;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.25rem;
    margin-top: 2rem;
  }

  .pricing-card {
    padding: 2rem 1.75rem;
    background: linear-gradient(180deg, #131313 0%, #0f0f0f 100%);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 16px;
    display: flex;
    flex-direction: column;
    position: relative;
    transition: all 0.3s;
  }

  .pricing-card:hover {
    border-color: rgba(255, 255, 255, 0.12);
    transform: translateY(-2px);
  }

  .pricing-card.featured {
    border-color: rgba(93, 181, 133, 0.3);
    background: linear-gradient(180deg, rgba(93, 181, 133, 0.06) 0%, rgba(93, 181, 133, 0.01) 100%);
    box-shadow: 0 0 40px rgba(93, 181, 133, 0.06), 0 8px 30px rgba(0, 0, 0, 0.3);
  }

  .pricing-card.featured:hover {
    border-color: rgba(93, 181, 133, 0.5);
    box-shadow: 0 0 50px rgba(93, 181, 133, 0.1), 0 12px 40px rgba(0, 0, 0, 0.4);
  }

  .featured-badge {
    position: absolute;
    top: -0.75rem;
    right: 1.25rem;
    padding: 0.3rem 0.75rem;
    background: #5DB585;
    color: #0a0a0a;
    font-size: 0.6875rem;
    font-weight: 700;
    border-radius: 6px;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    box-shadow: 0 2px 8px rgba(93, 181, 133, 0.3);
  }

  .pricing-header h3 {
    font-size: 1.125rem;
    font-weight: 700;
    margin-bottom: 0.25rem;
  }

  .pricing-header p {
    font-size: 0.8125rem;
    color: #666;
    margin: 0;
  }

  .price {
    font-size: 2.75rem;
    font-weight: 800;
    margin: 1.25rem 0;
    color: #fff;
    letter-spacing: -0.03em;
  }

  .price-period {
    font-size: 1rem;
    font-weight: 500;
    color: #666;
  }

  .feature-list {
    list-style: none;
    padding: 0;
    margin: 0 0 1.75rem;
    flex-grow: 1;
  }

  .feature-list li {
    padding: 0.625rem 0;
    padding-left: 1.5rem;
    position: relative;
    font-size: 0.9375rem;
    color: #bbb;
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
  }

  .feature-list li:last-child {
    border-bottom: none;
  }

  .feature-list li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: #5DB585;
    font-weight: 600;
  }

  /* Pricing Buttons */
  .btn-pricing {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    padding: 0.9375rem 1rem;
    background: rgba(255, 255, 255, 0.06);
    color: #ccc;
    font-weight: 700;
    font-size: 0.9375rem;
    font-family: inherit;
    border-radius: 10px;
    border: 1px solid rgba(255, 255, 255, 0.08);
    cursor: pointer;
    transition: all 0.25s;
  }

  .btn-pricing:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.15);
    color: #fff;
  }

  .btn-pricing.featured {
    background: #5DB585;
    color: #0a0a0a;
    border: none;
    box-shadow: 0 0 20px rgba(93, 181, 133, 0.15);
  }

  .btn-pricing.featured:hover {
    background: #6FC494;
    box-shadow: 0 0 30px rgba(93, 181, 133, 0.25);
    transform: translateY(-1px);
  }

  .free-preview {
    text-align: center;
    margin-top: 1.75rem;
    font-size: 0.9375rem;
    color: #666;
  }

  .free-preview a {
    color: #5DB585;
    text-decoration: none;
    transition: color 0.2s;
  }

  .free-preview a:hover {
    color: #7EDBA8;
  }

  .free-preview .arrow {
    display: inline-block;
    transition: transform 0.2s;
  }

  .free-preview a:hover .arrow {
    transform: translateX(3px);
  }

  /* ============================================
     FAQ SECTION
     ============================================ */
  .faq-section {
    padding: 3rem 0;
  }

  .faq-section h2 {
    margin-bottom: 1.75rem;
  }

  .faq-grid {
    display: grid;
    gap: 0;
  }

  .faq-item {
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }

  .faq-item:first-child {
    border-top: 1px solid rgba(255, 255, 255, 0.06);
  }

  .faq-item summary {
    padding: 1.125rem 0;
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    list-style: none;
    transition: color 0.2s;
  }

  .faq-item summary:hover {
    color: #5DB585;
  }

  .faq-item summary::-webkit-details-marker {
    display: none;
  }

  .faq-item summary::after {
    content: "+";
    font-size: 1.25rem;
    color: #444;
    transition: all 0.3s;
    width: 28px;
    height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.03);
    flex-shrink: 0;
  }

  .faq-item[open] summary::after {
    transform: rotate(45deg);
    color: #5DB585;
    background: rgba(93, 181, 133, 0.08);
  }

  .faq-answer {
    padding-bottom: 1.25rem;
  }

  .faq-answer p {
    color: #888;
    font-size: 0.9375rem;
    line-height: 1.7;
    margin: 0;
  }

  /* ============================================
     EMAIL SECTION
     ============================================ */
  .email-section {
    padding: 2rem 0 3.5rem;
  }

  /* ============================================
     RESPONSIVE
     ============================================ */
  @media (max-width: 768px) {
    .value-grid,
    .pricing-grid {
      grid-template-columns: 1fr;
    }

    .engine-grid {
      grid-template-columns: 1fr;
    }

    .contents-grid {
      grid-template-columns: 1fr 1fr;
    }

    .hero {
      padding: 3rem 0 2.5rem;
    }

    .hero-stats {
      gap: 1rem;
    }
  }

  @media (max-width: 480px) {
    .contents-grid {
      grid-template-columns: 1fr;
    }

    .hero h1 {
      font-size: 1.875rem;
    }

    .hero-stats {
      gap: 0.75rem;
    }

    .hero-stat-value {
      font-size: 1rem;
    }
  }
</style>
