---
import SidebarLayout from '../../layouts/SidebarLayout.astro';

const images = {
  hero: '/cases/images/hero-kircher-atlantis-map-1665.jpg',
  santorini: '/cases/images/atmosphere-santorini-caldera.jpg',
  timaeus: '/cases/images/evidence-timaeus-manuscript-medieval.jpg',
  minoanFleet: '/cases/images/evidence-minoan-fleet-fresco-akrotiri.jpg',
  richat: '/cases/images/evidence-richat-structure-satellite.jpg',
  plato: '/cases/images/figure-plato-bust-capitoline.jpg',
  donnelly: '/cases/images/figure-ignatius-donnelly.jpg',
};
---

<SidebarLayout
  title="Atlantis Book — Lost Civilization Mystery"
  description="Atlantis: Deconstructing Plato's original myth with evidence-based analysis. Non-fiction ebook exploring the lost civilization theory. $2.99 EPUB."
  currentVolume={9}
  image="/books/covers/atlantis.jpg"
  ogType="book"
  keywords="Atlantis book, Atlantis ebook, lost civilization, Plato Atlantis, ancient mysteries, lost worlds non-fiction, Atlantis evidence, sunken city, Richat Structure"
  bookSeo={{
    bookTitle: "Atlantis",
    subtitle: "Deconstructing Plato's Original Myth",
    price: "2.99",
    author: "HistorIQly Thrillers",
    genre: "Non-Fiction, History, Ancient Mysteries, Lost Civilizations",
    format: "EBook",
    keywords: "Atlantis, Plato, lost civilization, ancient mystery",
  }}
  breadcrumbs={[
    { name: "Home", url: "/" },
    { name: "Lost Worlds", url: "/vol/lost-worlds" },
    { name: "Atlantis", url: "/cases/atlantis" },
  ]}
>
  <article class="case-file">

    <!-- HERO SECTION -->
    <header class="hero">
      <div class="hero-bg">
        <img src={images.hero} alt="" />
        <div class="hero-overlay"></div>
      </div>

      <div class="hero-content">
        <div class="case-meta">
          <span class="meta-badge preview">FREE</span>
          <span class="meta-badge">CASE 01-360 BCE</span>
          <span class="meta-badge alert">STATUS: MYTHOLOGICAL</span>
        </div>

        <h1 class="hero-title">Atlantis</h1>
        <p class="hero-subtitle">Deconstructing Plato's Original Myth</p>

        <div class="hero-stats">
          <div class="stat">
            <span class="stat-label">Written</span>
            <span class="stat-value">c. 360 BCE</span>
          </div>
          <div class="stat">
            <span class="stat-label">Source</span>
            <span class="stat-value">Plato</span>
          </div>
           <div class="stat">
            <span class="stat-label">Confidence</span>
            <span class="stat-value text-accent">95%</span>
          </div>
        </div>

        <div class="hero-actions">
          <a href="/books/#book" class="btn-hero-action">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
              <polyline points="7 10 12 15 17 10"></polyline>
              <line x1="12" y1="15" x2="12" y2="3"></line>
            </svg>
            <span>Get Free EPUB</span>
          </a>
        </div>
      </div>

       <div class="scroll-indicator">
        <span>INVESTIGATE</span>
        <div class="scroll-line"></div>
      </div>
    </header>

    <!-- THE HOOK -->
    <section class="intro-section">
      <div class="intro-text">
        <p class="drop-cap">Everyone knows the story of Atlantis — the island paradise that sank beneath the waves. Almost nobody has read the original. What Plato actually wrote is stranger, more political, and more deliberately fictional than the legend suggests.</p>
        <p>A philosopher's parable. A 2,400-year misunderstanding. And the <strong>myth that refuses to die</strong>.</p>
      </div>
    </section>

    <!-- BENTO GRID STATS -->
    <section class="bento-section">
      <div class="bento-grid">

        <div class="bento-card large-card discovery-card">
          <div class="card-content">
            <span class="card-label">The Source</span>
            <h3 class="card-value">2 Texts</h3>
            <p class="card-desc">The entire Atlantis story comes from just two of Plato's dialogues — the <em>Timaeus</em> and the <em>Critias</em>. No other ancient source independently corroborates the account.</p>
          </div>
        </div>

        <div class="bento-card check-card">
          <div class="card-content">
            <span class="card-label">Claimed Date</span>
            <h3 class="card-value text-accent">9,000 yr</h3>
            <p class="card-desc">Before Solon — placing events in 9600 BCE, deep in the Mesolithic.</p>
          </div>
        </div>

        <div class="bento-card papers-card">
          <div class="card-content">
            <span class="card-label">Search Expeditions</span>
            <h3 class="card-value">Dozens</h3>
            <p class="card-desc">From the Sahara to the Caribbean, centuries of searching have found nothing.</p>
          </div>
        </div>

        <div class="bento-card fraud-card">
          <div class="card-content">
            <span class="card-label">Proposed Locations</span>
            <h3 class="card-value">40+</h3>
            <p class="card-desc">Santorini, Sardinia, Antarctica, Bimini, the Richat Structure — everyone sees Atlantis where they want to.</p>
          </div>
        </div>

      </div>
    </section>

    <!-- FORENSIC EVIDENCE -->
    <section class="evidence-section">
      <div class="section-header">
        <span class="section-label">Exhibit A</span>
        <h2 class="section-title">The Evidence</h2>
      </div>

      <div class="evidence-grid">
        <!-- Item 1 -->
        <div class="evidence-card">
          <div class="evidence-visual">
            <img src={images.timaeus} alt="Medieval manuscript of Plato's Timaeus" />
            <div class="visual-overlay">
              <span class="overlay-label">PRIMARY SOURCE</span>
            </div>
          </div>
          <div class="evidence-data">
            <h3>The Only Source</h3>
            <p>Every detail about Atlantis traces back to two dialogues by Plato, written around 360 BCE. The <em>Critias</em> breaks off mid-sentence — in the middle of Zeus's speech. No Egyptian record of the story has ever been found.</p>
          </div>
        </div>

        <!-- Item 2 -->
        <div class="evidence-card">
          <div class="evidence-visual">
            <img src={images.minoanFleet} alt="Minoan fleet fresco from Akrotiri, Thera" />
            <div class="visual-overlay">
               <span class="overlay-label">THERA THEORY</span>
            </div>
          </div>
          <div class="evidence-data">
            <h3>The Minoan Connection</h3>
            <p>The eruption of Thera around 1600 BCE destroyed the Minoan settlement at Akrotiri. The Thera-Atlantis theory is seductive — but requires changing the date, location, size, and civilisation Plato described.</p>
          </div>
        </div>

        <!-- Item 3 -->
        <div class="evidence-card">
          <div class="evidence-visual">
            <img src={images.richat} alt="Richat Structure satellite image, Mauritania" />
            <div class="visual-overlay">
               <span class="overlay-label">DEBUNKED</span>
            </div>
          </div>
          <div class="evidence-data">
            <h3>The Richat Structure</h3>
            <p>A 40-kilometre geological formation in the Sahara with concentric rings. Popular on YouTube, dismissed by geologists — it's 100 million years old, has never been underwater, and contains no artefacts.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- TIMELINE -->
    <section class="timeline-section">
      <div class="section-header">
        <span class="section-label">Chronology</span>
        <h2 class="section-title">The Life of a Myth</h2>
      </div>

      <div class="timeline">
        <div class="timeline-line"></div>

        <div class="timeline-entry left">
          <span class="timeline-year">c. 360 BCE</span>
          <div class="timeline-content">
            <h4>Plato Writes</h4>
            <p>Plato composes the <em>Timaeus</em> and <em>Critias</em>, introducing the story of Atlantis as a philosophical parable about power, corruption, and divine justice.</p>
          </div>
        </div>

        <div class="timeline-entry right">
          <span class="timeline-year">c. 330 BCE</span>
          <div class="timeline-content">
            <h4>Aristotle's Verdict</h4>
            <p>"The man who dreamed it up also made it disappear." Plato's own student treats the story as fiction.</p>
          </div>
        </div>

        <div class="timeline-entry left">
          <span class="timeline-year">1665</span>
          <div class="timeline-content">
            <h4>Kircher's Map</h4>
            <p>Athanasius Kircher publishes the first "map" of Atlantis — placed in the mid-Atlantic with no evidence. The image becomes iconic.</p>
          </div>
        </div>

        <div class="timeline-entry right">
          <span class="timeline-year">1882</span>
          <div class="timeline-content highlight">
            <h4>Donnelly's Bestseller</h4>
            <p>Ignatius Donnelly publishes <em>Atlantis: The Antediluvian World</em>, launching the modern obsession. Every fringe theory since traces its roots here.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- KEY FIGURES -->
    <section class="figures-section">
      <div class="section-header">
        <span class="section-label">Key Figures</span>
        <h2 class="section-title">The Philosopher and the Believer</h2>
      </div>

      <div class="figures-grid">
        <div class="figure-card">
          <div class="figure-image">
            <img src={images.plato} alt="Marble bust of Plato, Capitoline Museum" />
          </div>
          <div class="figure-info">
            <h3>Plato</h3>
            <span class="figure-role">c. 428–348 BCE</span>
            <p>Athenian philosopher who invented the Atlantis story as a vehicle for his political philosophy. He never claimed to have discovered a real place — he constructed a parable about what happens when a powerful state loses its virtue.</p>
          </div>
        </div>

        <div class="figure-card">
          <div class="figure-image">
            <img src={images.donnelly} alt="Ignatius Donnelly, author of Atlantis: The Antediluvian World" />
          </div>
          <div class="figure-info">
            <h3>Ignatius Donnelly</h3>
            <span class="figure-role">1831–1901</span>
            <p>Minnesota congressman who published the 1882 bestseller that launched modern Atlantis mania. His claim that all civilisation derived from a single Atlantic island laid the groundwork for a century of pseudoarchaeology.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CONCLUSION -->
    <section class="conclusion-section">
      <div class="conclusion-grid">
        <div class="conclusion-image">
          <img src={images.santorini} alt="Santorini caldera, remnant of the ancient Thera eruption" />
          <div class="image-caption">Santorini's caldera — the volcano that didn't inspire Atlantis.</div>
        </div>
        <div class="conclusion-text">
          <h2>The Real Question</h2>
          <p>It isn't <em>where</em> Atlantis was. It's why we keep looking. Plato created a story so compelling that it has been mistaken for fact for two and a half thousand years.</p>
          <p>The real Atlantis is not on the ocean floor. It exists where it has always existed — in the pages of a philosopher who understood that the most powerful truths are sometimes the ones you have to <strong>invent</strong>.</p>

          <div class="cta-box">
            <h3>Get the Full Book — Free</h3>
            <p>8 chapters. The complete story of Plato's invention, the believers who took it literally, and the real drowned cities that may have inspired it.</p>
            <a href="/books/#book" class="btn-download">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                <polyline points="7 10 12 15 17 10"></polyline>
                <line x1="12" y1="15" x2="12" y2="3"></line>
              </svg>
              Get Free EPUB
            </a>
          </div>
        </div>
      </div>
    </section>

  </article>
</SidebarLayout>

<style>
  /* SWISS TECHNICAL BENTO THEME */
  :root {
    --bg-dark: #0a0a0a;
    --bg-card: #111111;
    --bg-card-hover: #161616;
    --border-color: rgba(255, 255, 255, 0.08);
    --text-primary: #FAFAFA;
    --text-secondary: #A1A1AA;
    --accent: #F59E0B;
    --accent-dim: rgba(245, 158, 11, 0.1);

    --font-serif: 'Cormorant Garamond', Georgia, serif;
    --font-sans: 'Inter', system-ui, sans-serif;
  }

  .case-file {
    background-color: var(--bg-dark);
    color: var(--text-primary);
    font-family: var(--font-sans);
    overflow-x: hidden;
  }

  /* UTILS */
  .text-accent { color: var(--accent); }

  /* HERO */
  .hero {
    position: relative;
    height: 90vh;
    min-height: 600px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    border-bottom: 1px solid var(--border-color);
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-bg img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.4) grayscale(0.8) contrast(1.2);
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, transparent 0%, var(--bg-dark) 90%);
  }

  .hero-content {
    position: relative;
    z-index: 10;
    text-align: center;
    max-width: 900px;
    padding: 0 1.5rem;
  }

  .case-meta {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 2rem;
  }

  .meta-badge {
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    padding: 0.4rem 0.8rem;
    border: 1px solid var(--border-color);
    background: rgba(0,0,0,0.5);
    backdrop-filter: blur(4px);
    color: var(--text-secondary);
  }

  .meta-badge.alert {
    border-color: var(--accent);
    color: var(--accent);
    background: var(--accent-dim);
  }

  .meta-badge.preview {
    border-color: rgba(255, 255, 255, 0.4);
    background: rgba(255, 255, 255, 0.1);
    color: #fff;
  }

  .hero-title {
    font-family: var(--font-serif);
    font-size: clamp(3.5rem, 8vw, 6rem);
    line-height: 0.9;
    font-weight: 600;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
    text-shadow: 0 10px 30px rgba(0,0,0,0.7);
  }

  .hero-subtitle {
    font-size: clamp(1.1rem, 3vw, 1.5rem);
    color: var(--text-secondary);
    font-weight: 300;
    margin-bottom: 4rem;
    letter-spacing: 0.02em;
  }

  .hero-stats {
    display: flex;
    justify-content: center;
    gap: 3rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(255,255,255,0.1);
  }

  .stat {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .stat-label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-secondary);
    margin-bottom: 0.25rem;
  }

  .stat-value {
    font-family: var(--font-serif);
    font-size: 1.5rem;
    font-weight: 500;
  }

  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    z-index: 10;
  }

  .scroll-indicator span {
    font-size: 0.65rem;
    letter-spacing: 0.3em;
    color: var(--text-secondary);
    opacity: 0.7;
  }

  .scroll-line {
    width: 1px;
    height: 60px;
    background: linear-gradient(to bottom, var(--text-secondary) 0%, transparent 100%);
  }

  /* HERO ACTIONS */
  .hero-actions {
    margin-top: 3rem;
  }

  .btn-hero-action {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1.5rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid var(--border-color);
    color: var(--text-primary);
    text-decoration: none;
    font-family: var(--font-ui);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border-radius: 4px;
    backdrop-filter: blur(4px);
    transition: all 0.2s;
  }

  .btn-hero-action:hover {
    background: var(--accent);
    color: var(--bg-dark);
    border-color: var(--accent);
    transform: translateY(-2px);
  }

  /* INTRO */
  .intro-section {
    padding: 8rem 1.5rem;
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .intro-text p {
    font-family: var(--font-serif);
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    line-height: 1.6;
    color: var(--text-secondary);
    margin-bottom: 2rem;
  }

  .intro-text strong {
    color: var(--text-primary);
    font-weight: 600;
  }

  /* BENTO GRID */
  .bento-section {
    padding: 2rem 1.5rem 8rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .bento-grid {
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    gap: 1.5rem;
  }

  @media (min-width: 768px) {
    .bento-grid {
      grid-template-columns: repeat(3, 1fr);
      grid-template-rows: repeat(2, minmax(200px, auto));
    }

    .large-card {
      grid-column: span 2;
      grid-row: span 2;
    }
  }

  .bento-card {
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    padding: 2rem;
    border-radius: 4px;
    transition: transform 0.3s ease, border-color 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .bento-card:hover {
    transform: translateY(-2px);
    border-color: rgba(255,255,255,0.2);
    background: var(--bg-card-hover);
  }

  .card-label {
    display: block;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-secondary);
    margin-bottom: 1rem;
    border-bottom: 1px solid --border-color;
  }

  .card-value {
    font-family: var(--font-serif);
    font-size: 3rem;
    font-weight: 400;
    margin-bottom: 0.5rem;
    line-height: 1;
  }

  .discovery-card .card-value { font-size: 5rem; font-weight: 700; }
  .check-card .card-value { font-size: 4rem; }

  .card-desc {
    font-size: 0.95rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  /* EVIDENCE */
  .evidence-section {
    padding: 4rem 1.5rem;
    background: #0F0F0F;
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-label {
    font-size: 0.75rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--accent);
    display: block;
    margin-bottom: 0.5rem;
  }

  .section-title {
    font-family: var(--font-serif);
    font-size: 3rem;
    font-weight: 400;
  }

  .evidence-grid {
    max-width: 1000px;
    margin: 0 auto;
    display: grid;
    gap: 4rem;
  }

  .evidence-card {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    align-items: center;
  }

  @media (min-width: 768px) {
    .evidence-card {
      grid-template-columns: 1fr 1fr;
    }
    .evidence-card:nth-child(even) .evidence-visual {
      order: 2;
    }
  }

  .evidence-visual {
    position: relative;
    border: 1px solid var(--border-color);
  }

  .evidence-visual img {
    width: 100%;
    display: block;
    filter: brightness(0.8) contrast(1.1);
  }

  .visual-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 0.5rem;
    background: rgba(0,0,0,0.7);
    backdrop-filter: blur(2px);
    border-top: 1px solid rgba(255,255,255,0.1);
  }

  .overlay-label {
    font-size: 0.6rem;
    font-family: monospace;
    color: var(--accent);
    letter-spacing: 0.1em;
  }

  .evidence-data h3 {
    font-family: var(--font-serif);
    font-size: 2rem;
    margin-bottom: 1rem;
    font-weight: 500;
  }

  .evidence-data p {
    color: var(--text-secondary);
    line-height: 1.7;
    font-size: 1.1rem;
  }

  /* KEY FIGURES */
  .figures-section {
    padding: 6rem 1.5rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .figures-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  @media (min-width: 768px) {
    .figures-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .figure-card {
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: 4px;
    overflow: hidden;
    transition: transform 0.3s ease, border-color 0.3s ease;
  }

  .figure-card:hover {
    transform: translateY(-2px);
    border-color: rgba(255,255,255,0.2);
  }

  .figure-image img {
    width: 100%;
    aspect-ratio: 3/4;
    object-fit: cover;
    filter: brightness(0.8) contrast(1.1) grayscale(0.3);
  }

  .figure-info {
    padding: 1.5rem;
  }

  .figure-info h3 {
    font-family: var(--font-serif);
    font-size: 1.5rem;
    margin-bottom: 0.25rem;
  }

  .figure-role {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--accent);
    display: block;
    margin-bottom: 1rem;
  }

  .figure-info p {
    font-size: 0.95rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* TIMELINE */
  .timeline-section {
    padding: 6rem 1.5rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .timeline {
    position: relative;
    padding: 2rem 0;
  }

  .timeline-line {
    position: absolute;
    left: 50%;
    top: 0;
    bottom: 0;
    width: 1px;
    background: var(--border-color);
    transform: translateX(-50%);
  }

  .timeline-entry {
    position: relative;
    display: flex;
    justify-content: center;
    margin-bottom: 4rem;
  }

  .timeline-year {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    background: var(--bg-dark);
    padding: 0.25rem 0.5rem;
    font-family: monospace;
    color: var(--accent);
    font-size: 0.8rem;
    border: 1px solid var(--border-color);
    z-index: 2;
  }

  .timeline-content {
    width: 45%;
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    padding: 1.5rem;
    position: relative;
  }

  .timeline-entry.left .timeline-content {
    margin-right: auto;
    text-align: right;
  }

  .timeline-entry.right .timeline-content {
    margin-left: auto;
    text-align: left;
  }

  .timeline-content h4 {
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.5rem;
    color: var(--text-primary);
  }

  .timeline-content p {
    font-size: 0.9rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  .timeline-content.highlight {
    border-color: var(--accent);
    background: var(--accent-dim);
  }

  @media (max-width: 768px) {
    .timeline-line { left: 20px; }
    .timeline-entry { flex-direction: column; padding-left: 50px; }
    .timeline-year { left: 0; transform: none; top: -10px; width: 40px; text-align: center; }
    .timeline-content { width: 100%; text-align: left !important; }
    .timeline-entry.left .timeline-content, .timeline-entry.right .timeline-content { margin: 0; }
  }

  /* CONCLUSION */
  .conclusion-section {
    padding: 6rem 1.5rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .conclusion-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;
    align-items: center;
  }

  @media (min-width: 900px) {
    .conclusion-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .conclusion-image img {
    width: 100%;
    border-radius: 4px;
    filter: sepia(0.2) contrast(1.1);
  }

  .image-caption {
    margin-top: 1rem;
    font-family: monospace;
    font-size: 0.8rem;
    color: var(--text-secondary);
    text-align: center;
    border-top: 1px solid var(--border-color);
    padding-top: 0.5rem;
  }

  .conclusion-text h2 {
    font-family: var(--font-serif);
    font-size: 3rem;
    margin-bottom: 2rem;
  }

  .conclusion-text p {
    font-size: 1.2rem;
    line-height: 1.6;
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
  }

  .cta-box {
    margin-top: 3rem;
    padding: 2rem;
    border: 1px solid var(--border-color);
    background: var(--bg-card);
    text-align: center;
  }

  .cta-box h3 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .cta-box p {
    font-size: 1rem;
    margin-bottom: 2rem;
  }

  .btn-download {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    background: var(--accent);
    color: #000;
    padding: 1rem 2rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    text-decoration: none;
    border-radius: 4px;
    transition: transform 0.2s;
  }

  .btn-download:hover {
    transform: translateY(-2px);
    background: #D97706;
  }

  /* Drop Cap */
  .drop-cap:first-letter {
    font-size: 4rem;
    float: left;
    margin-right: 1rem;
    line-height: 0.8;
    font-family: var(--font-serif);
    color: var(--accent);
  }
</style>
