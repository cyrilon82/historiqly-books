---
import SidebarLayout from '../../layouts/SidebarLayout.astro';

const stripeUrl = import.meta.env.PUBLIC_STRIPE_BUY_URL;

const images = {
  hero: '/cases/images/hero-antikythera-mechanism.jpg',
  fragmentBack: '/cases/images/evidence-antikythera-fragment-back.jpg',
  closeup: '/cases/images/evidence-antikythera-closeup.jpg',
  gearsDetail: '/cases/images/evidence-antikythera-gears-detail.jpg',
  modelFront: '/cases/images/evidence-antikythera-model-front.jpg',
  inscriptions: '/cases/images/evidence-antikythera-inscriptions.jpg',
  museumDisplay: '/cases/images/evidence-antikythera-museum-display.jpg',
  price: '/cases/images/figure-derek-de-solla-price.jpg',
  ephebe: '/cases/images/figure-antikythera-ephebe.jpg',
};
---

<SidebarLayout title="The Antikythera Mechanism" description="The Ancient Computer That Shouldn't Exist" currentVolume={10}>
  <article class="case-file">

    <!-- HERO SECTION -->
    <header class="hero">
      <div class="hero-bg">
        <img src={images.hero} alt="" />
        <div class="hero-overlay"></div>
      </div>

      <div class="hero-content">
        <div class="case-meta">
          <span class="meta-badge price">$2.99</span>
          <span class="meta-badge">CASE 02 — c. 150 BCE</span>
          <span class="meta-badge alert">STATUS: AUTHENTIC</span>
        </div>

        <h1 class="hero-title">The Antikythera Mechanism</h1>
        <p class="hero-subtitle">The Ancient Computer That Shouldn't Exist</p>

        <div class="hero-stats">
          <div class="stat">
            <span class="stat-label">Built</span>
            <span class="stat-value">c. 150 BCE</span>
          </div>
          <div class="stat">
            <span class="stat-label">Found</span>
            <span class="stat-value">1901</span>
          </div>
           <div class="stat">
            <span class="stat-label">Gears</span>
            <span class="stat-value text-accent">37+</span>
          </div>
        </div>

        <div class="hero-actions">
          <button onclick={`localStorage.setItem('historiqly_purchasing','antikythera-mechanism');window.location.href='${stripeUrl}'`} class="btn-buy-primary" id="heroBuyBtn">
            <span class="btn-buy-text">Buy the Book</span>
            <span class="btn-buy-price">$2.99</span>
          </button>
          <a href="/books/antikythera-mechanism.epub" class="btn-download-primary" id="heroDownloadBtn" style="display:none;" download>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
              <polyline points="7 10 12 15 17 10"></polyline>
              <line x1="12" y1="15" x2="12" y2="3"></line>
            </svg>
            <span>Download Your Book</span>
          </a>
        </div>
      </div>

       <div class="scroll-indicator">
        <span>INVESTIGATE</span>
        <div class="scroll-line"></div>
      </div>
    </header>

    <!-- THE HOOK -->
    <section class="intro-section">
      <div class="intro-text">
        <p class="drop-cap">In 1901, sponge divers hauled a corroded lump of bronze from a Roman shipwreck off a tiny Greek island. It sat in a museum storeroom for months before anyone noticed the impossible thing inside it: a gear. Then another. Then thirty more.</p>
        <p>It took a century to understand what they had found — a <strong>2,000-year-old computer</strong> that could predict eclipses, track the planets, and time the Olympic Games.</p>
      </div>
    </section>

    <!-- BENTO GRID STATS -->
    <section class="bento-section">
      <div class="bento-grid">

        <div class="bento-card large-card discovery-card">
          <div class="card-content">
            <span class="card-label">The Device</span>
            <h3 class="card-value">37+</h3>
            <p class="card-desc">Interlocking bronze gear wheels, hand-cut with triangular teeth, housed in a wooden case the size of a shoebox. The largest gear has 223 teeth — one for each month in the Saros eclipse cycle.</p>
          </div>
        </div>

        <div class="bento-card check-card">
          <div class="card-content">
            <span class="card-label">Depth</span>
            <h3 class="card-value text-accent">60m</h3>
            <p class="card-desc">The depth at which sponge divers found the wreck. One diver died. Two were permanently paralysed.</p>
          </div>
        </div>

        <div class="bento-card papers-card">
          <div class="card-content">
            <span class="card-label">Fragments</span>
            <h3 class="card-value">82</h3>
            <p class="card-desc">Surviving pieces, from Fragment A (the main drive gear) to fragments smaller than a fingernail.</p>
          </div>
        </div>

        <div class="bento-card fraud-card">
          <div class="card-content">
            <span class="card-label">Inscriptions</span>
            <h3 class="card-value">3,000+</h3>
            <p class="card-desc">Characters of Greek text discovered on the mechanism's surfaces — a user manual for operating the ancient computer.</p>
          </div>
        </div>

      </div>
    </section>

    <!-- FORENSIC EVIDENCE -->
    <section class="evidence-section">
      <div class="section-header">
        <span class="section-label">Exhibit A</span>
        <h2 class="section-title">The Evidence</h2>
      </div>

      <div class="evidence-grid">
        <!-- Item 1 -->
        <div class="evidence-card">
          <div class="evidence-visual">
            <img src={images.gearsDetail} alt="Internal gear wheels of the Antikythera mechanism" />
            <div class="visual-overlay">
              <span class="overlay-label">GEAR TRAIN ANALYSIS</span>
            </div>
          </div>
          <div class="evidence-data">
            <h3>The Gears</h3>
            <p>At least 37 interlocking bronze wheels, each hand-cut from sheet metal roughly 2mm thick. The gear ratios encode specific astronomical cycles — the 19-year Metonic cycle, the 223-month Saros eclipse cycle, and the variable speed of the moon's orbit. Nothing this complex was built again for over a thousand years.</p>
          </div>
        </div>

        <!-- Item 2 -->
        <div class="evidence-card">
          <div class="evidence-visual">
            <img src={images.inscriptions} alt="Greek inscriptions on the Antikythera mechanism" />
            <div class="visual-overlay">
               <span class="overlay-label">SURFACE INSCRIPTIONS</span>
            </div>
          </div>
          <div class="evidence-data">
            <h3>The User Manual</h3>
            <p>Over 3,000 characters of Greek text were discovered using CT scanning and polynomial texture mapping. The inscriptions describe how to operate the device, what each dial displays, and even the predicted colours of eclipses. Some letters are barely 1.2mm tall — engraved with astonishing precision.</p>
          </div>
        </div>

        <!-- Item 3 -->
        <div class="evidence-card">
          <div class="evidence-visual">
            <img src={images.modelFront} alt="Modern reconstruction of the Antikythera mechanism" />
            <div class="visual-overlay">
               <span class="overlay-label">MODERN RECONSTRUCTION</span>
            </div>
          </div>
          <div class="evidence-data">
            <h3>The Reconstruction</h3>
            <p>Modern researchers have built working replicas showing the mechanism could track the sun, moon, and five known planets through the zodiac, predict solar and lunar eclipses, display the moon's phase, and even schedule the Olympic Games. It was a portable model of the cosmos.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- TIMELINE -->
    <section class="timeline-section">
      <div class="section-header">
        <span class="section-label">Chronology</span>
        <h2 class="section-title">Two Thousand Years</h2>
      </div>

      <div class="timeline">
        <div class="timeline-line"></div>

        <div class="timeline-entry left">
          <span class="timeline-year">c. 150 BCE</span>
          <div class="timeline-content">
            <h4>Construction</h4>
            <p>The mechanism is built, probably on the island of Rhodes, using astronomical knowledge from Hipparchus and Babylonian eclipse records. The Corinthian calendar inscribed on its dials connects it to Syracuse — the home of Archimedes.</p>
          </div>
        </div>

        <div class="timeline-entry right">
          <span class="timeline-year">c. 60 BCE</span>
          <div class="timeline-content highlight">
            <h4>The Shipwreck</h4>
            <p>A Roman cargo ship carrying Greek art, statues, and luxury goods sinks near the island of Antikythera. The mechanism, already an antique, goes down with it. It will rest on the seabed for two millennia.</p>
          </div>
        </div>

        <div class="timeline-entry left">
          <span class="timeline-year">1901</span>
          <div class="timeline-content">
            <h4>The Discovery</h4>
            <p>Sponge divers from Symi discover the wreck at 60 metres. They recover statues, coins, jewellery — and a corroded lump of bronze that nobody thinks to examine closely. One diver dies in the salvage operation.</p>
          </div>
        </div>

        <div class="timeline-entry right">
          <span class="timeline-year">1902</span>
          <div class="timeline-content">
            <h4>The Gear</h4>
            <p>Archaeologist Valerios Stais spots a gear wheel through a crack in the corrosion. He proposes it's an ancient astronomical computer. Scholars dismiss the idea — the ancient Greeks didn't build machines like this.</p>
          </div>
        </div>

        <div class="timeline-entry left">
          <span class="timeline-year">1974</span>
          <div class="timeline-content">
            <h4>Gears from the Greeks</h4>
            <p>Derek de Solla Price publishes his landmark study, identifying 30+ gears and proving the device modelled celestial motions. The corroded lump becomes one of the most important archaeological finds in history.</p>
          </div>
        </div>

        <div class="timeline-entry right">
          <span class="timeline-year">2005</span>
          <div class="timeline-content highlight">
            <h4>The CT Revolution</h4>
            <p>An eight-tonne CT scanner is shipped from England to Athens. It reveals the mechanism's full complexity: 37+ gears, 3,000+ characters of inscriptions, eclipse predictions, planetary tracking, and the schedule of the Olympic Games.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- KEY FIGURES -->
    <section class="figures-section">
      <div class="section-header">
        <span class="section-label">Dossier</span>
        <h2 class="section-title">Key Figures</h2>
      </div>

      <div class="figures-grid">
        <div class="figure-card">
          <div class="figure-image">
            <img src={images.price} alt="Derek de Solla Price" />
          </div>
          <div class="figure-info">
            <span class="figure-role">The Detective</span>
            <h3>Derek de Solla Price</h3>
            <p>British-American physicist who spent decades studying the mechanism. His 1974 monograph <em>Gears from the Greeks</em> proved it was an ancient computer. He died in 1983, before the full extent of its secrets were revealed.</p>
          </div>
        </div>

        <div class="figure-card">
          <div class="figure-image">
            <img src={images.ephebe} alt="The Antikythera Youth bronze statue" />
          </div>
          <div class="figure-info">
            <span class="figure-role">The Shipwreck</span>
            <h3>The Antikythera Youth</h3>
            <p>A magnificent bronze statue recovered from the same shipwreck. The Youth became the famous face of the Antikythera wreck — while the corroded lump of gears sat unnoticed in a storeroom for decades.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CONCLUSION -->
    <section class="conclusion-section">
      <div class="conclusion-grid">
        <div class="conclusion-image">
          <img src={images.museumDisplay} alt="The Antikythera mechanism on display at the National Archaeological Museum, Athens" />
          <div class="image-caption">The Antikythera mechanism today. National Archaeological Museum, Athens.</div>
        </div>
        <div class="conclusion-text">
          <h2>What Else Have We Lost?</h2>
          <p>The mechanism survived by pure chance — a storm, a shipwreck, a sponge diver, a crack in the corrosion. Nothing comparable would be built for another thousand years.</p>
          <p>It doesn't just challenge what we know about ancient technology. It challenges <em>what we think we know about what was possible.</em></p>

          <div class="cta-box" id="ctaBox">
            <h3 id="ctaTitle">Get the Full Book</h3>
            <p>The complete story of the shipwreck, the gears, and the century-long quest to decode the world's first computer.</p>
            <button onclick={`localStorage.setItem('historiqly_purchasing','antikythera-mechanism');window.location.href='${stripeUrl}'`} class="btn-buy-primary cta-buy" id="ctaBuyBtn">
              <span class="btn-buy-text">Buy the Book</span>
              <span class="btn-buy-price">$2.99</span>
            </button>
            <a href="/books/antikythera-mechanism.epub" class="btn-download-primary cta-download" id="ctaDownloadBtn" style="display:none;" download>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
                <polyline points="7 10 12 15 17 10"></polyline>
                <line x1="12" y1="15" x2="12" y2="3"></line>
              </svg>
              Download Your Book
            </a>
          </div>
        </div>
      </div>
    </section>

  </article>
</SidebarLayout>

<script>
  // Purchase detection (fallback — homepage handles the main redirect)
  const params = new URLSearchParams(window.location.search);
  if (params.get('purchased') === 'true') {
    localStorage.setItem('historiqly_purchased_antikythera-mechanism', 'true');
    localStorage.removeItem('historiqly_purchasing');
    history.replaceState({}, '', window.location.pathname);
  }

  const hasPurchased = localStorage.getItem('historiqly_purchased_antikythera-mechanism') === 'true';

  if (hasPurchased) {
    // Hero buttons
    const heroBuy = document.getElementById('heroBuyBtn');
    const heroDownload = document.getElementById('heroDownloadBtn');
    if (heroBuy) heroBuy.style.display = 'none';
    if (heroDownload) heroDownload.style.display = 'inline-flex';

    // CTA buttons
    const ctaBuy = document.getElementById('ctaBuyBtn');
    const ctaDownload = document.getElementById('ctaDownloadBtn');
    const ctaTitle = document.getElementById('ctaTitle');
    if (ctaBuy) ctaBuy.style.display = 'none';
    if (ctaDownload) ctaDownload.style.display = 'inline-flex';
    if (ctaTitle) ctaTitle.textContent = 'Your Book Is Ready';
  }
</script>

<style>
  /* SWISS TECHNICAL BENTO THEME */
  :root {
    --bg-dark: #0a0a0a;
    --bg-card: #111111;
    --bg-card-hover: #161616;
    --border-color: rgba(255, 255, 255, 0.08);
    --text-primary: #FAFAFA;
    --text-secondary: #A1A1AA;
    --accent: #F59E0B;
    --accent-dim: rgba(245, 158, 11, 0.1);

    --font-serif: 'Cormorant Garamond', Georgia, serif;
    --font-sans: 'Inter', system-ui, sans-serif;
  }

  .case-file {
    background-color: var(--bg-dark);
    color: var(--text-primary);
    font-family: var(--font-sans);
    overflow-x: hidden;
  }

  /* UTILS */
  .text-accent { color: var(--accent); }

  /* HERO */
  .hero {
    position: relative;
    height: 90vh;
    min-height: 600px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    border-bottom: 1px solid var(--border-color);
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-bg img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.4) grayscale(0.8) contrast(1.2);
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, transparent 0%, var(--bg-dark) 90%);
  }

  .hero-content {
    position: relative;
    z-index: 10;
    text-align: center;
    max-width: 900px;
    padding: 0 1.5rem;
  }

  .case-meta {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 2rem;
  }

  .meta-badge {
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    padding: 0.4rem 0.8rem;
    border: 1px solid var(--border-color);
    background: rgba(0,0,0,0.5);
    backdrop-filter: blur(4px);
    color: var(--text-secondary);
  }

  .meta-badge.alert {
    border-color: var(--accent);
    color: var(--accent);
    background: var(--accent-dim);
  }

  .meta-badge.price {
    border-color: var(--accent);
    background: var(--accent-dim);
    color: var(--accent);
    font-weight: 800;
  }

  .hero-title {
    font-family: var(--font-serif);
    font-size: clamp(3rem, 7vw, 5.5rem);
    line-height: 0.9;
    font-weight: 600;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
    text-shadow: 0 10px 30px rgba(0,0,0,0.7);
  }

  .hero-subtitle {
    font-size: clamp(1.1rem, 3vw, 1.5rem);
    color: var(--text-secondary);
    font-weight: 300;
    margin-bottom: 4rem;
    letter-spacing: 0.02em;
  }

  .hero-stats {
    display: flex;
    justify-content: center;
    gap: 3rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(255,255,255,0.1);
  }

  .stat {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .stat-label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-secondary);
    margin-bottom: 0.25rem;
  }

  .stat-value {
    font-family: var(--font-serif);
    font-size: 1.5rem;
    font-weight: 500;
  }

  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    z-index: 10;
  }

  .scroll-indicator span {
    font-size: 0.65rem;
    letter-spacing: 0.3em;
    color: var(--text-secondary);
    opacity: 0.7;
  }

  .scroll-line {
    width: 1px;
    height: 60px;
    background: linear-gradient(to bottom, var(--text-secondary) 0%, transparent 100%);
  }

  /* HERO ACTIONS */
  .hero-actions {
    margin-top: 3rem;
  }

  .btn-buy-primary {
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem 2.5rem;
    background: linear-gradient(135deg, #F59E0B 0%, #D97706 100%);
    color: #000;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    font-family: var(--font-sans);
    transition: all 0.25s;
    box-shadow: 0 4px 24px rgba(245, 158, 11, 0.3), 0 2px 8px rgba(0, 0, 0, 0.3);
  }

  .btn-buy-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 32px rgba(245, 158, 11, 0.45), 0 4px 12px rgba(0, 0, 0, 0.3);
    background: linear-gradient(135deg, #FBBF24 0%, #F59E0B 100%);
  }

  .btn-buy-text {
    font-size: 1rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .btn-buy-price {
    font-size: 1.125rem;
    font-weight: 800;
    padding: 0.25rem 0.75rem;
    background: rgba(0, 0, 0, 0.15);
    border-radius: 4px;
  }

  .btn-download-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2.5rem;
    background: linear-gradient(135deg, #5DB585 0%, #4a9e70 100%);
    color: #000;
    text-decoration: none;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-family: var(--font-sans);
    transition: all 0.25s;
    box-shadow: 0 4px 24px rgba(93, 181, 133, 0.3), 0 2px 8px rgba(0, 0, 0, 0.3);
  }

  .btn-download-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 32px rgba(93, 181, 133, 0.45), 0 4px 12px rgba(0, 0, 0, 0.3);
    background: linear-gradient(135deg, #6FC494 0%, #5DB585 100%);
  }

  .cta-buy, .cta-download {
    width: 100%;
    justify-content: center;
  }

  /* INTRO */
  .intro-section {
    padding: 8rem 1.5rem;
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .intro-text p {
    font-family: var(--font-serif);
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    line-height: 1.6;
    color: var(--text-secondary);
    margin-bottom: 2rem;
  }

  .intro-text strong {
    color: var(--text-primary);
    font-weight: 600;
  }

  /* BENTO GRID */
  .bento-section {
    padding: 2rem 1.5rem 8rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .bento-grid {
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    gap: 1.5rem;
  }

  @media (min-width: 768px) {
    .bento-grid {
      grid-template-columns: repeat(3, 1fr);
      grid-template-rows: repeat(2, minmax(200px, auto));
    }

    .large-card {
      grid-column: span 2;
      grid-row: span 2;
    }
  }

  .bento-card {
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    padding: 2rem;
    border-radius: 4px;
    transition: transform 0.3s ease, border-color 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .bento-card:hover {
    transform: translateY(-2px);
    border-color: rgba(255,255,255,0.2);
    background: var(--bg-card-hover);
  }

  .card-label {
    display: block;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-secondary);
    margin-bottom: 1rem;
  }

  .card-value {
    font-family: var(--font-serif);
    font-size: 3rem;
    font-weight: 400;
    margin-bottom: 0.5rem;
    line-height: 1;
  }

  .discovery-card .card-value { font-size: 5rem; font-weight: 700; }
  .check-card .card-value { font-size: 4rem; }

  .card-desc {
    font-size: 0.95rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  /* EVIDENCE */
  .evidence-section {
    padding: 4rem 1.5rem;
    background: #0F0F0F;
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-label {
    font-size: 0.75rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--accent);
    display: block;
    margin-bottom: 0.5rem;
  }

  .section-title {
    font-family: var(--font-serif);
    font-size: 3rem;
    font-weight: 400;
  }

  .evidence-grid {
    max-width: 1000px;
    margin: 0 auto;
    display: grid;
    gap: 4rem;
  }

  .evidence-card {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    align-items: center;
  }

  @media (min-width: 768px) {
    .evidence-card {
      grid-template-columns: 1fr 1fr;
    }
    .evidence-card:nth-child(even) .evidence-visual {
      order: 2;
    }
  }

  .evidence-visual {
    position: relative;
    border: 1px solid var(--border-color);
  }

  .evidence-visual img {
    width: 100%;
    display: block;
    filter: brightness(0.8) contrast(1.1);
  }

  .visual-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 0.5rem;
    background: rgba(0,0,0,0.7);
    backdrop-filter: blur(2px);
    border-top: 1px solid rgba(255,255,255,0.1);
  }

  .overlay-label {
    font-size: 0.6rem;
    font-family: monospace;
    color: var(--accent);
    letter-spacing: 0.1em;
  }

  .evidence-data h3 {
    font-family: var(--font-serif);
    font-size: 2rem;
    margin-bottom: 1rem;
    font-weight: 500;
  }

  .evidence-data p {
    color: var(--text-secondary);
    line-height: 1.7;
    font-size: 1.1rem;
  }

  /* TIMELINE */
  .timeline-section {
    padding: 6rem 1.5rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .timeline {
    position: relative;
    padding: 2rem 0;
  }

  .timeline-line {
    position: absolute;
    left: 50%;
    top: 0;
    bottom: 0;
    width: 1px;
    background: var(--border-color);
    transform: translateX(-50%);
  }

  .timeline-entry {
    position: relative;
    display: flex;
    justify-content: center;
    margin-bottom: 4rem;
  }

  .timeline-year {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    background: var(--bg-dark);
    padding: 0.25rem 0.5rem;
    font-family: monospace;
    color: var(--accent);
    font-size: 0.8rem;
    border: 1px solid var(--border-color);
    z-index: 2;
    white-space: nowrap;
  }

  .timeline-content {
    width: 45%;
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    padding: 1.5rem;
    position: relative;
  }

  .timeline-entry.left .timeline-content {
    margin-right: auto;
    text-align: right;
  }

  .timeline-entry.right .timeline-content {
    margin-left: auto;
    text-align: left;
  }

  .timeline-content h4 {
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.5rem;
    color: var(--text-primary);
  }

  .timeline-content p {
    font-size: 0.9rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  .timeline-content.highlight {
    border-color: var(--accent);
    background: var(--accent-dim);
  }

  @media (max-width: 768px) {
    .timeline-line { left: 20px; }
    .timeline-entry { flex-direction: column; padding-left: 50px; }
    .timeline-year { left: 0; transform: none; top: -10px; width: auto; text-align: center; }
    .timeline-content { width: 100%; text-align: left !important; }
    .timeline-entry.left .timeline-content, .timeline-entry.right .timeline-content { margin: 0; }
  }

  /* KEY FIGURES */
  .figures-section {
    padding: 6rem 1.5rem;
    background: #0F0F0F;
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
  }

  .figures-grid {
    max-width: 1000px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  @media (min-width: 768px) {
    .figures-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .figure-card {
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: 4px;
    overflow: hidden;
    transition: transform 0.3s ease, border-color 0.3s ease;
  }

  .figure-card:hover {
    transform: translateY(-2px);
    border-color: rgba(255,255,255,0.2);
  }

  .figure-image {
    aspect-ratio: 3/4;
    overflow: hidden;
  }

  .figure-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: grayscale(0.6) contrast(1.1) brightness(0.8);
    transition: filter 0.3s ease;
  }

  .figure-card:hover .figure-image img {
    filter: grayscale(0.3) contrast(1.1) brightness(0.9);
  }

  .figure-info {
    padding: 1.5rem;
  }

  .figure-role {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--accent);
    display: block;
    margin-bottom: 0.5rem;
  }

  .figure-info h3 {
    font-family: var(--font-serif);
    font-size: 1.75rem;
    font-weight: 500;
    margin-bottom: 0.75rem;
  }

  .figure-info p {
    font-size: 0.9rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* CONCLUSION */
  .conclusion-section {
    padding: 6rem 1.5rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .conclusion-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;
    align-items: center;
  }

  @media (min-width: 900px) {
    .conclusion-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .conclusion-image img {
    width: 100%;
    border-radius: 4px;
    filter: sepia(0.2) contrast(1.1);
  }

  .image-caption {
    margin-top: 1rem;
    font-family: monospace;
    font-size: 0.8rem;
    color: var(--text-secondary);
    text-align: center;
    border-top: 1px solid var(--border-color);
    padding-top: 0.5rem;
  }

  .conclusion-text h2 {
    font-family: var(--font-serif);
    font-size: 3rem;
    margin-bottom: 2rem;
  }

  .conclusion-text p {
    font-size: 1.2rem;
    line-height: 1.6;
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
  }

  .cta-box {
    margin-top: 3rem;
    padding: 2rem;
    border: 1px solid var(--border-color);
    background: var(--bg-card);
    text-align: center;
  }

  .cta-box h3 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .cta-box p {
    font-size: 1rem;
    margin-bottom: 2rem;
  }


  /* Drop Cap */
  .drop-cap:first-letter {
    font-size: 4rem;
    float: left;
    margin-right: 1rem;
    line-height: 0.8;
    font-family: var(--font-serif);
    color: var(--accent);
  }
</style>
