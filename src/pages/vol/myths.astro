---
import SidebarLayout from '../../layouts/SidebarLayout.astro';
import EmailCapture from '../../components/EmailCapture.astro';
const stripeUrl = import.meta.env.PUBLIC_STRIPE_BUY_URL;
---

<SidebarLayout title="Vol 2: Historical Myths Debunked" currentVolume={2}>
  <div class="volume-page">
    <div class="volume-header">
      <div class="volume-header-glow"></div>
      <div class="volume-header-inner">
        <div class="volume-meta">
          <span class="badge coming-soon"><span class="badge-dot"></span>Coming Soon</span>
          <span class="volume-number">Vol 2</span>
        </div>
        <h1>Historical Myths Debunked</h1>
        <p class="volume-subtitle">Separating fact from legend</p>
      </div>
    </div>

    <div class="volume-intro">
      <div class="intro-accent"></div>
      <p>
        What you learned in school might be wrong. This volume examines popular historical "facts" that don't survive scrutiny.
        From misattributed quotes to events that never happened, we separate documented history from persistent mythology.
      </p>
    </div>

    <div class="section-divider"></div>

    <div class="cases-section" id="cases">
      <div class="cases-header">
        <h2>Coming Soon</h2>
        <span class="cases-count">7 cases</span>
      </div>
      <div class="cases-preview">
        <div class="case-preview"><span class="case-num">01</span><span class="case-title">Did Cleopatra Die by Snake Bite?</span><div class="case-btns"><a href="/cases/cleopatra-snake-bite" class="btn-read">Read Case</a><button onclick={`window.location.href='${stripeUrl}'`} class="btn-buy-sm">Buy $2.99</button></div></div>
        <div class="case-preview"><span class="case-num">02</span><span class="case-title">Did Viking Warriors Wear Horned Helmets?</span><div class="case-btns"><a href="/cases/viking-horned-helmets" class="btn-read">Read Case</a><button onclick={`window.location.href='${stripeUrl}'`} class="btn-buy-sm">Buy $2.99</button></div></div>
        <div class="case-preview"><span class="case-num">03</span><span class="case-title">Were Pirates Really Like Hollywood Shows?</span><div class="case-btns"><a href="/cases/hollywood-pirates" class="btn-read">Read Case</a><button onclick={`window.location.href='${stripeUrl}'`} class="btn-buy-sm">Buy $2.99</button></div></div>
        <div class="case-preview"><span class="case-num">04</span><span class="case-title">Were Ninjas Real?</span><span class="btn-soon">Coming Soon</span></div>
        <div class="case-preview"><span class="case-num">05</span><span class="case-title">Was the Medieval Period Really "Dark"?</span><span class="btn-soon">Coming Soon</span></div>
        <div class="case-preview"><span class="case-num">06</span><span class="case-title">Did Medieval Torturers Use Iron Maidens?</span><span class="btn-soon">Coming Soon</span></div>
        <div class="case-preview"><span class="case-num">07</span><span class="case-title">Did Gladiators Die by Thumbs Down?</span><span class="btn-soon">Coming Soon</span></div>
      </div>
    </div>

    <div class="email-section"><EmailCapture /></div>

    <div class="available-now">
      <p>While you wait, check out:</p>
      <a href="/vol/hoaxes" class="vol-link">
        <span class="vol-badge soon">Soon</span>
        <span>Vol 1: Hoaxes, Forgeries & Frauds</span>
        <span class="arrow">→</span>
      </a>
    </div>
  </div>
</SidebarLayout>

<style>
  .volume-page { max-width: 820px; margin: 0 auto; }
  .volume-header { position: relative; margin-bottom: 2.5rem; padding: 2.5rem 0 0; overflow: hidden; }
  .volume-header-glow { position: absolute; top: -60px; left: 50%; transform: translateX(-50%); width: 500px; height: 300px; background: radial-gradient(ellipse, rgba(93, 181, 133, 0.06) 0%, transparent 70%); pointer-events: none; }
  .volume-header-inner { position: relative; z-index: 1; }
  .volume-meta { display: flex; align-items: center; gap: 0.75rem; margin-bottom: 1.25rem; }
  .badge { display: inline-flex; align-items: center; gap: 0.4rem; padding: 0.375rem 0.875rem; border-radius: 100px; font-size: 0.6875rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.06em; }
  .badge.coming-soon { background: rgba(255, 255, 255, 0.05); border: 1px solid rgba(255, 255, 255, 0.08); color: #999; }
  .badge-dot { width: 5px; height: 5px; background: #999; border-radius: 50%; animation: pulse-dot 2s ease-in-out infinite; }
  @keyframes pulse-dot { 0%, 100% { opacity: 0.4; } 50% { opacity: 1; } }
  .volume-number { font-size: 0.6875rem; font-weight: 700; color: #5DB585; text-transform: uppercase; letter-spacing: 0.1em; }
  .volume-header h1 { font-size: clamp(2rem, 5vw, 2.75rem); font-weight: 800; margin-bottom: 0.5rem; line-height: 1.1; letter-spacing: -0.03em; }
  .volume-subtitle { color: #777; font-size: 1.0625rem; line-height: 1.5; }
  .volume-intro { padding: 1.75rem; background: linear-gradient(180deg, #131313 0%, #0f0f0f 100%); border: 1px solid rgba(255, 255, 255, 0.06); border-radius: 14px; margin-bottom: 2rem; position: relative; overflow: hidden; }
  .intro-accent { position: absolute; top: 0; left: 0; width: 3px; height: 100%; background: linear-gradient(180deg, #5DB585, rgba(93, 181, 133, 0.2)); border-radius: 3px 0 0 3px; }
  .volume-intro p { font-size: 1.0625rem; line-height: 1.8; color: #bbb; margin: 0; padding-left: 0.75rem; }
  .section-divider { height: 1px; background: linear-gradient(90deg, transparent, rgba(93, 181, 133, 0.12), transparent); margin: 0.5rem 0 2rem; }
  .cases-header { display: flex; align-items: baseline; gap: 0.75rem; margin-bottom: 1.25rem; }
  .cases-section h2 { font-size: 1.5rem; font-weight: 800; letter-spacing: -0.02em; }
  .cases-count { font-size: 0.75rem; color: #555; font-weight: 500; }
  .cases-preview { display: flex; flex-direction: column; gap: 0.5rem; margin-bottom: 2.5rem; }
  .case-preview { display: flex; align-items: center; gap: 1rem; padding: 1rem 1.25rem; background: linear-gradient(180deg, #131313 0%, #0f0f0f 100%); border: 1px solid rgba(255, 255, 255, 0.06); border-radius: 12px; color: #bbb; transition: all 0.25s; }
  .case-preview:hover { border-color: rgba(93, 181, 133, 0.2); transform: translateX(4px); background: linear-gradient(180deg, #151515 0%, #111111 100%); }
  .case-num { display: inline-flex; align-items: center; justify-content: center; min-width: 32px; height: 28px; padding: 0 0.375rem; background: rgba(93, 181, 133, 0.08); border-radius: 6px; font-size: 0.75rem; font-weight: 800; color: #5DB585; flex-shrink: 0; }
  .case-title { font-weight: 500; }
  .email-section { margin-bottom: 2rem; }
  .available-now { text-align: center; margin-bottom: 1rem; }
  .available-now > p { color: #666; font-size: 0.875rem; margin-bottom: 0.75rem; }
  .vol-link { display: inline-flex; align-items: center; gap: 0.75rem; padding: 0.875rem 1.5rem; background: rgba(93, 181, 133, 0.05); border: 1px solid rgba(93, 181, 133, 0.2); border-radius: 12px; text-decoration: none; color: #ccc; font-size: 0.9375rem; font-weight: 500; transition: all 0.25s; }
  .vol-link:hover { background: rgba(93, 181, 133, 0.1); border-color: rgba(93, 181, 133, 0.4); color: #fff; }
  .vol-badge { padding: 0.2rem 0.5rem; font-size: 0.5625rem; font-weight: 700; text-transform: uppercase; border-radius: 4px; letter-spacing: 0.02em; }
  .vol-badge.soon { background: rgba(255, 255, 255, 0.08); color: #888; }
  .arrow { color: #5DB585; transition: transform 0.2s; display: inline-block; }
  .vol-link:hover .arrow { transform: translateX(3px); }
  .case-btns { display: flex; gap: 0.5rem; margin-left: auto; flex-shrink: 0; }
  .btn-read { display: inline-flex; align-items: center; padding: 0.375rem 0.75rem; background: rgba(93, 181, 133, 0.1); border: 1px solid rgba(93, 181, 133, 0.3); color: #5DB585; font-weight: 600; font-size: 0.6875rem; border-radius: 6px; text-decoration: none; transition: all 0.2s; white-space: nowrap; }
  .btn-read:hover { background: rgba(93, 181, 133, 0.2); border-color: rgba(93, 181, 133, 0.5); color: #6FC494; }
  .btn-buy-sm { display: inline-flex; align-items: center; padding: 0.375rem 0.75rem; background: linear-gradient(135deg, #F59E0B 0%, #D97706 100%); color: #000; font-weight: 700; font-size: 0.6875rem; border-radius: 6px; border: none; cursor: pointer; transition: all 0.2s; white-space: nowrap; }
  .btn-buy-sm:hover { background: linear-gradient(135deg, #FBBF24 0%, #F59E0B 100%); box-shadow: 0 2px 8px rgba(245, 158, 11, 0.3); }
  .btn-soon { margin-left: auto; color: #555; font-size: 0.6875rem; font-weight: 500; white-space: nowrap; flex-shrink: 0; }
  @media (max-width: 480px) { .volume-header h1 { font-size: 1.75rem; } .case-preview { padding: 0.875rem 1rem; gap: 0.75rem; } }
</style>
