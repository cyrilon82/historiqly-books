---
import SidebarLayout from '../../layouts/SidebarLayout.astro';
import EmailCapture from '../../components/EmailCapture.astro';
// import SubscriptionModal from '../../components/SubscriptionModal.astro'; // Enable after launch
---

<SidebarLayout title="Vol 4: Famous Disappearances" currentVolume={4}>
  <div class="volume-page">
    <div class="volume-header">
      <span class="badge coming-soon">Coming Soon</span>
      <h1>Famous Disappearances</h1>
      <p class="volume-subtitle">Volume 4 of the AI Casefiles Series</p>
    </div>

    <div class="volume-intro">
      <p>
        Where did they go? From aviators to aristocrats, some people vanish without a trace. 
        We examine the evidence behind history's most puzzling disappearances, separating plausible theories from pure speculation.
      </p>
    </div>

    <div class="cases-section">
      <h2>Planned Casefiles</h2>
      <div class="cases-preview">
        <div class="case-preview"><span class="case-num">01</span><span>Amelia Earhart</span></div>
        <div class="case-preview"><span class="case-num">02</span><span>D.B. Cooper</span></div>
        <div class="case-preview"><span class="case-num">03</span><span>The Mary Celeste</span></div>
        <div class="case-preview"><span class="case-num">04</span><span>Malaysia Airlines MH370</span></div>
        <div class="case-preview"><span class="case-num">05</span><span>The Lost Colony of Roanoke</span></div>
        <div class="case-preview"><span class="case-num">06</span><span>The Princes in the Tower</span></div>
        <div class="case-preview"><span class="case-num">07</span><span>Jimmy Hoffa</span></div>
      </div>
    </div>

    <EmailCapture />

    <div class="available-now">
      <p>While you wait:</p>
      <a href="/vol/hoaxes" class="vol-link">
        <span class="vol-badge soon">Soon</span>
        <span>Vol 1: Hoaxes, Forgeries & Frauds</span>
        <span class="arrow">→</span>
      </a>
    </div>
  </div>
</SidebarLayout>

<style>
  .volume-page { max-width: 900px; }
  .volume-header { margin-bottom: 2rem; }
  .badge { display: inline-block; padding: 0.375rem 0.875rem; border-radius: 100px; font-size: 0.75rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 1rem; background: rgba(255, 255, 255, 0.08); color: #a8a8a8; }
  .volume-header h1 { font-size: clamp(2rem, 5vw, 3rem); font-weight: 800; margin-bottom: 0.5rem; line-height: 1.1; }
  .volume-subtitle { color: #a8a8a8; font-size: 1.125rem; }
  .volume-intro { padding: 1.5rem; background: #111111; border: 1px solid rgba(255, 255, 255, 0.08); border-radius: 12px; margin-bottom: 3rem; }
  .volume-intro p { font-size: 1.125rem; line-height: 1.8; color: #e0e0e0; }
  .cases-section h2 { font-size: 1.5rem; font-weight: 700; margin-bottom: 1.5rem; }
  .cases-preview { display: flex; flex-direction: column; gap: 0.5rem; margin-bottom: 3rem; }
  .case-preview { display: flex; align-items: center; gap: 1rem; padding: 1rem 1.25rem; background: #111111; border: 1px solid rgba(255, 255, 255, 0.08); border-radius: 8px; color: #a8a8a8; }
  .case-num { display: inline-block; padding: 0.25rem 0.5rem; background: #161616; border-radius: 4px; font-size: 0.75rem; font-weight: 700; color: #777; }
  .subscribe-section { text-align: center; padding: 2.5rem 2rem; background: #111111; border: 1px solid rgba(255, 255, 255, 0.08); border-radius: 16px; margin-bottom: 2rem; }
  .subscribe-section h3 { font-size: 1.5rem; margin-bottom: 0.5rem; }
  .subscribe-section > p { color: #a8a8a8; margin-bottom: 1.5rem; }
  .subscribe-options { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; max-width: 400px; margin: 0 auto; }
  .subscribe-card { display: flex; flex-direction: column; align-items: center; padding: 1.25rem; background: #161616; border: 1px solid #222; border-radius: 12px; text-decoration: none; transition: all 0.2s; position: relative; cursor: pointer; font-family: inherit; color: inherit; width: 100%; }
  .subscribe-card:hover { border-color: #5DB585; transform: translateY(-2px); }
  .subscribe-card.featured { border-color: #5DB585; background: rgba(93, 181, 133, 0.05); }
  .save-badge { position: absolute; top: -0.625rem; right: -0.5rem; padding: 0.25rem 0.5rem; background: #5DB585; color: #0a0a0a; font-size: 0.625rem; font-weight: 700; border-radius: 4px; text-transform: uppercase; }
  .plan-name { font-size: 0.875rem; font-weight: 600; color: #a8a8a8; margin-bottom: 0.25rem; }
  .plan-price { font-size: 1.5rem; font-weight: 800; color: #fff; }
  .plan-price .period { font-size: 0.875rem; font-weight: 500; color: #777; }
  .plan-desc { font-size: 0.75rem; color: #666; margin-top: 0.25rem; }
  @media (max-width: 480px) { .subscribe-options { grid-template-columns: 1fr; } }
  .available-now { text-align: center; }
  .available-now > p { color: #777; margin-bottom: 1rem; }
  .vol-link { display: inline-flex; align-items: center; gap: 0.75rem; padding: 1rem 1.5rem; background: rgba(93, 181, 133, 0.08); border: 1px solid rgba(93, 181, 133, 0.3); border-radius: 12px; text-decoration: none; color: #FAFAFA; transition: all 0.2s; }
  .vol-link:hover { background: rgba(93, 181, 133, 0.15); border-color: #5DB585; }
  .vol-badge { padding: 0.25rem 0.5rem; background: #5DB585; color: #0a0a0a; font-size: 0.625rem; font-weight: 700; text-transform: uppercase; border-radius: 4px; }
  .vol-badge.soon { background: rgba(255, 255, 255, 0.1); color: #a8a8a8; }
  .arrow { color: #5DB585; }
</style>
